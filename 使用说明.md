# 商品图片设计工具使用说明

这是一个本地网页工具，用来批量生成一组商品图片：主图、白色背景图、尺寸标注图、局部放大图、人物穿戴图、场景展示图、卖点展示图。图片规格可在页面顶部配置，默认请求 `1024x1024`，默认只强制 `1:1` 方图。

## 包内内容

```text
image-design-toolkit/
  image-design-workbench/          网页工具
  image-design-workbench/public/   页面文件
  image-design-workbench/server.js 本地服务和生图接口
  image-design-workbench/README.md 工具简要说明
  skills/custom-image-generator/   自定义文生图 skill
  使用说明.md
```

打包时不会包含 `.env`、API Key、历史生成图片、缓存文件。

## 运行环境

需要本机已安装：

- Node.js
- Python 3

不需要执行 `npm install`，这个工具没有外部 npm 依赖。

## 配置 API Key

解压后进入工具目录：

```bash
cd image-design-toolkit/image-design-workbench
```

创建 `.env` 文件：

```dotenv
CUSTOM_IMAGE_API_KEY=你的 API Key
CUSTOM_IMAGE_API_BASE=https://aicodelink.top/v1
CUSTOM_IMAGE_MODEL=gpt-image-2
```

也可以把 `.env` 放到：

```text
image-design-toolkit/skills/custom-image-generator/.env
```

两种方式都可以，建议优先放在 `image-design-workbench/.env`，方便跟工具放在一起管理。

## 启动工具

在 `image-design-workbench` 目录执行：

```bash
npm run dev
```

浏览器打开：

```text
http://127.0.0.1:4173
```

## 使用流程

1. 填写“主图”提示词并点击“生成主图”，或点击“上传主图”选择已有图片。
2. 主图准备好后，六张衍生图会解锁。
3. 如果上传了旧图，这张上传图会作为当前主图源图。
4. 分别填写白色背景图、尺寸标注图、局部放大图、人物穿戴图、场景展示图、卖点展示图提示词。
5. 点击“生成六张衍生图”，或单独点击某张图的“生成 / 重新生成”。
6. 可以下载单张图，也可以点击“下载全部”打包下载。

生成结果会保存到：

```text
image-design-workbench/generated-images/product-design/
```

## 重要说明

- 六张衍生图必须基于主图生成，所以没有主图时不能生成。
- 重新生成主图后，旧的衍生图会被标记为需要更新。
- 图片规格可在页面顶部配置；默认只要求 `1:1` 方图，选择“固定边长”后才会强制指定尺寸。
- 如果需要改端口，可以启动前设置 `PORT`：

```bash
PORT=4180 npm run dev
```

## 单独使用自定义文生图 skill

skill 脚本路径：

```text
skills/custom-image-generator/scripts/image_generator.py
```

示例：

```bash
python3 skills/custom-image-generator/scripts/image_generator.py "极简风产品主图，白底，高级商业摄影" --output-dir ./outputs --size 1024x1024
```

基于源图编辑：

```bash
python3 skills/custom-image-generator/scripts/image_generator.py "基于源图生成白色背景电商图" --endpoint edits --image ./main.png --output-dir ./outputs --size 1024x1024
```
